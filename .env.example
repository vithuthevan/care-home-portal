# Local Docker defaults. Copy to .env to override.
# These values are for local development only — do not use them in production.

# SQL Server SA password (must include upper, lower, digit, and a symbol).
MSSQL_SA_PASSWORD=CareHomeDevSql2022@
