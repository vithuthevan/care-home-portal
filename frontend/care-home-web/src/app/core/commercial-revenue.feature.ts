/** Commercial revenue cycle (AR, payments, banking, assurance). Set true to restore nav, routes, and UI links.
 * When enabling: set this flag to true, verify sidebar Revenue / Revenue Assurance sections in app.html,
 * confirm commercialRevenueGuard routes, dashboard outstanding KPI → /receivables, and invoice payment allocation handoff.
 */
export const COMMERCIAL_REVENUE_ENABLED = false;
